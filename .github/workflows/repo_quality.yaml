name: repo quality
on:
  push:
    branches:
      - dev
      - main
jobs:
  lock_file:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/install_uv
      - run: uv lock --locked

  type_consistency:
    runs-on: windows-latest
    needs: [lock_file]
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/install_uv
      - run: uv run --extra mt5 pyright .

  tests:
    runs-on: windows-latest
    needs: [lock_file]
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/install_uv
      - run: uv run --extra mt5 pytest -v
 
  build:
    runs-on: [ubuntu-latest]
    needs: [type_consistency, tests]
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/install_uv
      - run: uv build